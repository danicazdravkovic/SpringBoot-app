name: Push action
on: [push]
jobs:
  Start: 
    runs-on: ubuntu-latest
    steps: 
      - run: echo "Event that starts this action is ${{github.event_name}}"
  show_message:
    runs-on: ubuntu-latest
    steps:
      - name: Show message
        run: echo "Push event detected on main branch. This is a custom message."
      